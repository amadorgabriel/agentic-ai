# CV templates (local-only)

Add your Word templates here — they are gitignored:

- `master_cv_pt_template.docx` — PT master layout
- `master_cv_en_template.docx` — EN master layout (optional; generate via `scripts/make_en_template.py`)

Do not commit personal CV content to the public repo.
